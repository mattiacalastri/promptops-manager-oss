{
  "name": "promptops-manager-oss",
  "version": "1.0.0",
  "description": "Minimal desktop app for AI CLI terminal sessions and prompt catalog",
  "main": "electron/main.js",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "electron": "^33.0.0",
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
